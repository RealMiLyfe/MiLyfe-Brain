apiVersion: v1
kind: Service
metadata:
  name: milyfe-backend
  namespace: milyfe
  labels:
    app: milyfe-backend
spec:
  type: ClusterIP
  ports:
    - port: 8200
      targetPort: 8200
      protocol: TCP
      name: http
  selector:
    app: milyfe-backend
