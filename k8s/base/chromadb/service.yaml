apiVersion: v1
kind: Service
metadata:
  name: milyfe-chromadb
  namespace: milyfe
  labels:
    app: milyfe-chromadb
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: milyfe-chromadb
